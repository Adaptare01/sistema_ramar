# ============================================================
# Variáveis de Ambiente para Produção (KVM2 Hostinger)
# ============================================================
# INSTRUÇÃO: Estas variáveis são injetadas pelo CI/CD via GitHub Secrets.
# Nunca commitar com dados reais!
# ============================================================

# PostgreSQL — conexão principal
DATABASE_URL="postgresql://admin_ramar:Ramar%232025@infra_postgres:5432/ramar_sistema"

# JWT — chave secreta para autenticação (ALTERAR EM PRODUÇÃO!)
JWT_SECRET="change-me-to-a-strong-random-secret"

# App
NODE_ENV="production"
PORT="3000"
